Create package.json or import
1. Install GULP global: npm install gulpjs/gulp-cli -g
   for version: gulp -v
2. Install GULP local project folder: npm insatll gulp --save-dev
3. Create gulpfile.js or import
   Install GULP-Watch
   Create gulp task to pipe style to temp/style/
4. Install: npm install gulp-postcss --save-dev
   create var postcss = require('gulp-postcss');
